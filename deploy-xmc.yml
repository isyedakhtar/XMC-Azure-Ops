# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: windows-latest

steps:
- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '6.0.x'
    vsVersion: '2019'

- script: |
    dotnet tool restore
    dotnet sitecore --help
  displayName: 'XM CLoud Sitecore help'

- script: |
    dotnet sitecore cloud login --client-credentials --client-id bJzMmPaKMh203Vqo5htSgxcYeSN9bPTx --client-secret si78IxouaWsLMX9OptQtUtmf6-KUBIHea9J3akKAo6r6Ql8NHNOwb84r2Z-_PRfr --allow-write
  displayName: 'Login to XM Cloud'